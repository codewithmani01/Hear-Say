import { HearSayClient } from '@/components/hearsay-client';

export default function Home() {
  return (
    <main className="flex min-h-screen w-full items-center justify-center p-4 bg-gray-50">
      <HearSayClient />
    </main>
  );
}
